@extends('adminlte::page')
@section('title','Presença')
@section('content_header')<div class="d-flex justify-content-between align-items-center"><h1>Presença</h1><a href="{{ route('attendances.create') }}" class="btn btn-primary"><i class="bi bi-plus-lg"></i> Registrar presença</a></div>@stop
@section('content')<x-alerts /><div class="card mb-3"><div class="card-body"><form class="row g-2"><div class="col-md-6"><input name="search" value="{{ request('search') }}" class="form-control" placeholder="Buscar aluno"></div><div class="col-md-4"><input type="date" name="date" value="{{ request('date') }}" class="form-control"></div><div class="col-md-2"><button class="btn btn-primary w-100">Filtrar</button></div></form></div></div><div class="card"><div class="card-body table-responsive"><table class="table table-hover"><thead><tr><th>Aluno</th><th>Data</th><th>Entrada</th><th>Saída</th><th>Unidade</th></tr></thead><tbody>@forelse($attendances as $attendance)<tr><td>{{ $attendance->student->user->name }}</td><td>{{ $attendance->date->format('d/m/Y') }}</td><td>{{ $attendance->entry_time }}</td><td>{{ $attendance->exit_time ?: '-' }}</td><td>{{ $attendance->unit->name }}</td></tr>@empty<tr><td colspan="5" class="text-center">Nenhuma presença registrada.</td></tr>@endforelse</tbody></table></div><div class="card-footer d-flex justify-content-end">{{ $attendances->links() }}</div></div>@stop
