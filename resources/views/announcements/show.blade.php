@extends('adminlte::page')
@section('title', 'Detalhes do comunicado')
@section('content_header')<div class="d-flex justify-content-between align-items-center"><h1>{{ $announcement->title }}</h1><div class="d-flex gap-2"><a href="{{ route('announcements.index') }}" class="btn btn-secondary">Voltar</a><a href="{{ route('announcements.edit', $announcement) }}" class="btn btn-primary"><i class="bi bi-pencil"></i> Editar</a><form method="POST" action="{{ route('announcements.destroy', $announcement) }}" onsubmit="return confirm('Deseja excluir este comunicado?')">@csrf @method('DELETE')<button class="btn btn-danger"><i class="bi bi-trash"></i> Excluir</button></form></div></div>@stop
@section('content')<x-alerts /><div class="card"><div class="card-body"><dl class="row"><dt class="col-sm-3">Unidade</dt><dd class="col-sm-9">{{ $announcement->unit?->name ?? 'Todas' }}</dd><dt class="col-sm-3">Destinatários</dt><dd class="col-sm-9">{{ ucfirst($announcement->target_role ?? 'all') }}</dd><dt class="col-sm-3">Mensagem</dt><dd class="col-sm-9">{!! nl2br(e($announcement->message)) !!}</dd><dt class="col-sm-3">Período</dt><dd class="col-sm-9">{{ $announcement->start_at?->format('d/m/Y H:i') ?? 'Imediato' }} até {{ $announcement->end_at?->format('d/m/Y H:i') ?? 'Sem prazo' }}</dd><dt class="col-sm-3">Status</dt><dd class="col-sm-9">{{ $announcement->active ? 'Ativo' : 'Inativo' }}</dd></dl></div></div>@stop
